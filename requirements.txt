# app / worker コンテナ（Python 3.11）用
# スタックバージョンは vas に準拠（Django 4.2 / PostgreSQL 16 / gunicorn / uvicorn）

# --- Web / DB ---
Django==4.2
psycopg2==2.9.9
gunicorn==21.2.0
uvicorn==0.25.0
# 本番はフロント(vas nginx)を流用するため、静的ファイルは WhiteNoise でアプリ配信
whitenoise==6.6.0

# --- REST API（トークン認証） ---
djangorestframework==3.15.2

# --- 非同期ジョブ（Celery + Redis、直列処理） ---
celery==5.3.6
redis==5.0.1

# --- 認証強化（MFA 必須 / ログイン失敗ロックアウト） ---
django-otp==1.3.0
qrcode==7.4.2
django-axes==6.4.0

# --- ユーティリティ ---
requests==2.31.0
pandas==2.1.3
openpyxl==3.1.2

# --- HUHVar 解析エンジン（acmg-classifier, requires-python >=3.11） ---
# HUHVar ソースをボリュームマウントし entrypoint で `pip install -e /huhvar` する想定。
# （依存: click, cyvcf2, duckdb, pysam, pydantic, openspliceai, pyBigWig 等は pyproject から解決）
